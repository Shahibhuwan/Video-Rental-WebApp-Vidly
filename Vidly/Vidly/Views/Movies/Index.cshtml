@model IEnumerable<Vidly.Models.Movie>

    @{
        ViewBag.Title = "Movies";
        Layout = "~/Views/Shared/_Layout.cshtml";
    }

    <h2>Movies</h2>
    @if(!Model.Any())
    {
        <p>We don't have movie yet</p>
    }
    lse
    {
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>Movie</th>
                    <th>Genre</th>
                    <
                </tr>
            </thead>
            <tbody>
                @*Model is like a bag which contain all the database table sent from controller class*@
                @foreach (var movie in Model)

                {
                    <tr>
                        <td>@Html.ActionLink(movie.Name, "Edit", "Movies", new { id = movie.Id }, null)</td>
                        @*is used to send parameters in url to the specified controller for data representing *@
                        <td>@movie.Genre.Name</td>

                    </tr>
                }
            </tbody>
    </table>
}



